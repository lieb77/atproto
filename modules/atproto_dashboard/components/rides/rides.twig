{# Rides net.paullieberman.bike.ride #}

{{ attach_library('core/htmx') }}


<div class="pds-rides">
  {% if rides is not empty %}
    <table>
      <thead>
        <tr>
          <th>Rkey</th>
          <th>Created</th>
          <th>URL</th>
          <th colspan=3>Actions</th>
        </tr>
      </thead>
      <tbody>
        {% for ride in rides %}
        	{% include "atproto_dashboard:ride-row" with {
        		ride: ride
        	} only  %}        
        {% endfor %}
      </tbody>
    </table>
  {% else %}
    <p>No rides found on the PDS.</p>
  {% endif %}
</div>